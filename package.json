{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^7.0.1"
  },
  "scripts": {
    "postinstall": "lerna exec -- npm i",
    "release": "lerna publish",
    "start": "cd probot-app && npm run start"
  }
}
