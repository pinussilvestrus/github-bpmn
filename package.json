{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.10.7"
  },
  "scripts": {
    "pre-install": "lerna exec -- npm i",
    "release": "npm run pre-install && lerna publish"
  }
}
